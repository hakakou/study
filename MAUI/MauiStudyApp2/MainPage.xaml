<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauiStudyApp2"
             x:Class="MauiStudyApp2.MainPage">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Entry">
                <Setter Property="local:EntrySelection.SelectAllOnFocus" Value="True"/>
            </Style>
            <Style TargetType="Image">
                <Setter Property="local:DoubleTapToZoomBehavior.AttachBehavior" Value="True" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    
    <ScrollView >
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Image
                Source="dotnet_bot.png"
                HeightRequest="500"
                Aspect="AspectFit">
            </Image>

            <Entry local:EntrySelection.SelectAllOnFocus="True"
                x:Name="nameEntry"
                Placeholder="Enter your name"
                SemanticProperties.Hint="Input field for your name" />

            <Entry
                x:Name="emailEntry"
                Placeholder="Enter your email"
                Keyboard="Email"
                SemanticProperties.Hint="Input field for your email address" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
